import moment from "moment";

interface WeekColumnProps {
  day: string;
  tasks: any[];
  onTaskClick: (task: any) => void;
}

function WeekColumn() {
  return (
    <div className="week-grid">
    
      </div>
  );
}